import Phaser from "phaser";

// // const movesLeft = (moves: number) => `Moves Left: ${moves}`;

// export default class movesLeft extends Phaser.GameObjects.Text {
//   private movesLeft: number;

//   constructor(
//     scene: object,
//     x: number,
//     y: number,
//     moves: number,
//     style: object
//   ) {
//     super(scene, x, y, movesLeft(moves), style);
//     this.moves = moves;
//   }

//   decrementMoves() {
//     this.moves -= 1;
//     this.setText(movesLeft(this.moves));
//   }
// }
